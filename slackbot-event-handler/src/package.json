{
  "name": "hello-bolt-nodejs-aws-lambda",
  "version": "1.0.0",
  "description": "Deploying to AWS Lambda Bolt for JavaScript using the AwsLambdaReceiver",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.2.0"
  },
  "devDependencies": {
    "serverless": "^2.13.0",
    "serverless-offline": "^6.8.0"
  }
}
